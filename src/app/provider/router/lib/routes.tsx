export enum AppRoute {
  Main = "/",
  Used = "/used",
  Model = "/model",
  MyCars = "/mycars",
  MyCarsFavorites = "/favorites",
  MyCarsOrders = "/orders",
  MyCarsCalculations = "/calculations",
  NotFound = "/not-found",
}
